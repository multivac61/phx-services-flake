## phx-services-flake

To bootstrap an *hello world* project run `nix run`. This will generate a new folder `hello/` with a new phoenix project, initialize a postgres database and finally open a browser to access the newly created project.
